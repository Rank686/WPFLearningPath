<Window x:Class="WpfLearning.Step28.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Step 28 - ControlTemplate" Width="560" Height="360">
    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="18,10" /><Setter Property="FontSize" Value="15" />
        </Style>
        <Style x:Key="TemplatedButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="SteelBlue" /><Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="MidnightBlue" /><Setter Property="BorderThickness" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <StackPanel Margin="24">
        <TextBlock FontSize="22" FontWeight="SemiBold" Text="A template replaces the Button's visual structure" />
        <TextBlock Margin="0,8,0,20" Text="Border draws the shell; ContentPresenter keeps arbitrary Button content." />
        <Button HorizontalAlignment="Left" Style="{StaticResource TemplatedButtonStyle}" Click="TemplateButton_Click">
            <StackPanel Orientation="Horizontal"><TextBlock Text="★" Margin="0,0,8,0" /><TextBlock Text="Template button" /></StackPanel>
        </Button>
        <TextBlock x:Name="StatusText" Margin="0,18,0,0" FontWeight="SemiBold" Text="Not clicked" />
    </StackPanel>
</Window>
